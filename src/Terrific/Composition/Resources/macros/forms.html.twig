{# Here comes your macro definitions #}

{# Example input macro. This macro could be used as {{ forms.input(…) }} in your views -> namespace = filename (eg. forms)
    {% macro input(name, value, type, size) %}
        <input type="{{ type|default('text') }}" name="{{ name }}" value="{{ value|e }}" size="{{ size|default(20) }}" />
    {% endmacro %}
#}
